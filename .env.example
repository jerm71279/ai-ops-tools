# OberaConnect Unified Docker Stack - Environment Variables
# Copy this file to .env and fill in your values
# Usage: docker compose --env-file .env up -d

# =============================================================================
# CORE SERVICES (Required)
# =============================================================================

# Azure AD / Microsoft Graph
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# Anthropic API (Claude)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI API (optional, for GPT models)
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# PERSISTENCE LAYER
# =============================================================================

# PostgreSQL (chat/session persistence)
POSTGRES_USER=obera
POSTGRES_PASSWORD=change-this-secure-password
POSTGRES_DB=oberaconnect

# Qdrant (vector database)
QDRANT_API_KEY=your-qdrant-api-key

# =============================================================================
# N8N WORKFLOW AUTOMATION
# =============================================================================

N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-this-secure-password
N8N_ENCRYPTION_KEY=generate-a-32-char-random-string
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Webhook URL (for external integrations)
WEBHOOK_URL=http://localhost:5678

# =============================================================================
# LOCAL LLM (profile: local-llm)
# =============================================================================

# Ollama models to pull after startup
# Run: docker exec ollama ollama pull <model>
OLLAMA_MODELS=llama3.2,qwen2.5:7b

# GPU Configuration (NVIDIA)
# Set to 'all' for all GPUs, or specific GPU IDs like '0,1'
NVIDIA_VISIBLE_DEVICES=all

# =============================================================================
# CHAT UI (profile: chat)
# =============================================================================

# Open WebUI settings
WEBUI_SECRET_KEY=generate-a-random-secret-key
WEBUI_AUTH=true

# =============================================================================
# SSL/TLS (profile: ssl)
# =============================================================================

# Domain for SSL certificates
SSL_DOMAIN=localhost
SSL_EMAIL=admin@example.com

# =============================================================================
# NINJAONE INTEGRATION
# =============================================================================

NINJAONE_CLIENT_ID=your-ninjaone-client-id
NINJAONE_CLIENT_SECRET=your-ninjaone-client-secret
NINJAONE_INSTANCE=your-instance

# =============================================================================
# UNIFI INTEGRATION
# =============================================================================

UNIFI_HOST=unifi.ui.com
UNIFI_API_TOKEN=your-unifi-api-token

# =============================================================================
# OPTIONAL: SERVICE PORTS (defaults shown)
# =============================================================================

# Uncomment to override default ports
# GATEWAY_PORT=8080
# DATA_PROCESSING_PORT=8081
# RAG_ENGINE_PORT=8082
# ENGINEERING_API_PORT=8083
# CALL_FLOW_PORT=8084
# AGENTS_PORT=8085
# N8N_PORT=5678
# QDRANT_PORT=6333
# POSTGRES_PORT=5432
# OLLAMA_PORT=11434
# WEBUI_PORT=3000
